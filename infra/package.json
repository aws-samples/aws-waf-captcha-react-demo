{
  "name": "aws-waf-captcha-react",
  "version": "0.1.0",
  "bin": {
    "aws-waf-captcha-react": "bin/aws-waf-captcha-react.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest",
    "lint": "eslint --ext .js,.ts .",
    "format": "prettier --ignore-path .gitignore --write \"**/*.+(js|ts|json)\""
  },
  "devDependencies": {
    "aws-cdk": "^2.102.0",
    "jest": "^29.6.4"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.102.0",
    "constructs": "^10.0.0",
    "eslint": "^8.50.0",
    "eslint-config-standard": "^17.1.0",
    "prettier": "^3.0.3"
  }
}
